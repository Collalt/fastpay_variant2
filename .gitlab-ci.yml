stages:
  - test

integration_tests:
  stage: test
  image: python:3.11-slim
  before_script:
    - python -m pip install --upgrade pip
    - pip install -r requirements.txt
  script:
    - python demo.py
  artifacts:
    when: always
    paths:
      - docs/test-log.txt
